package Brains;

/**
 * So people can write an AI in a different language if they want to
 * Also would allow for a human interface if so desired
 */
public class NetworkPlayer extends Base_Brain {

}
